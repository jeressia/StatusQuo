import React from "react";
import Layout from "../../layout/Layout";

const Calendar = () => {
  const view = () => <div>Calendar</div>;

  return <Layout>{view()}</Layout>;
};

export default Calendar;
